version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - data:/app/server/data
      - uploads:/app/uploads
    environment:
      - KIMI_API_KEY=sk-kimi-你的key
      - PORT=3001
      - UPLOADS_DIR=/app/uploads

volumes:
  data:
  uploads:
